server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Necesario para React Router (rutas del lado cliente)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
